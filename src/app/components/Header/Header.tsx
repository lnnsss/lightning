import s from "./Header.module.css";
import {HeaderLayout} from "@/app/components/Header/miniComponents/HeaderLayout";

export const Header = () => {
    return (
        <HeaderLayout>

        </HeaderLayout>
    )
}